import { Router } from 'express';
import { validate } from '@middlewares/validator.middleware';

import { {{#each services}}{{this}}, {{/each}} } from '@modules/{{moduleMain}}/services/{{import}}.service';
import { create{{module}}Schema, update{{module}}Schema } from '@modules/{{moduleMain}}/validator/{{import}}.validator';

const router = Router();

router.get('/', {{services.[0]}});
router.get('/unique', {{services.[1]}});
router.post('/', validate(create{{module}}Schema), {{services.[2]}});
router.put('/', validate(update{{module}}Schema), {{services.[3]}});

export default router;